# Bloomberg MCP Gateway — compatibility lock (SPEC §1.6, Milestone 0).
# Generated by scripts/environment-probe.ps1 on the target workstation.
# All production dependencies are additionally locked in uv.lock.

python_version: "3.12.7"
blpapi_version: "3.26.7.1"
mcp_sdk_package: "mcp"
mcp_sdk_version: "2.0.0"
mcp_protocol_revision: "2026-07-28"
hermes_version: "0.20.2"
tailscale_version: "1.102.2"

# Verified in this environment probe:
#   - mcp 2.0.0 exposes MODERN_PROTOCOL_VERSIONS == ("2026-07-28",); the
#     streamable-HTTP manager routes that revision through the stateless
#     single-exchange path (no initialize handshake, no Mcp-Session-Id,
#     POST-only, MCP-Protocol-Version / Mcp-Method / Mcp-Name header
#     validation per mcp.shared.inbound.classify_inbound_request).
#   - blpapi 3.26.7.1 imports on Windows x64 with the bundled native library;
#     API surface introspected (Session, SessionOptions, Service, Operation,
#     SchemaElementDefinition, SchemaTypeDefinition, EventQueue,
#     SubscriptionList, CorrelationId, Name, DataType). See
#     docs/architecture.md "Native API notes" for the exact method set used.
#
# Pending on a logged-on Bloomberg Terminal (SPEC §5.2):
#   - BLPAPI connection proof to 127.0.0.1:8194
#   - Open-service proof and schema-introspection dumps
#   - Entitled-service list and installed-version operation limits (WAPI <GO>)
#   - Hermes bearer-header forwarding / structured-content / resource tests
